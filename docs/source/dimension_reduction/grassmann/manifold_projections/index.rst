ManifoldProjection
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

The abstract :class:`.ManifoldProjection` class is a blueprint for classes in :mod:`.manifold_projections` module. It allows the user to define a set of methods that must be created within any child classes built from this abstract class.

.. autoclass:: UQpy.dimension_reduction.grassmann_manifold.manifold_projections.baseclass.ManifoldProjection 
    :members:

